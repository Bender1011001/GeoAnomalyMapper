# Dashboard-specific dependencies for GeoAnomalyMapper
# These are for the Streamlit dashboard and related visualization tools.
# Extracted from pyproject.toml dashboard and visualization extras.
# Excludes core visualization like matplotlib and plotly (already in requirements.in).
# Excludes API deps like fastapi and uvicorn (in requirements-api.in).

# Streamlit dashboard
streamlit>=1.38.0
streamlit-folium>=0.17.0

# 3D and interactive visualization
itkwidgets>=0.13.0
pyvistaqt>=0.11.0
stpyvista>=0.1.0

# Additional visualization libraries
pygmt>=0.12.0
pyvista>=0.44.0
folium>=0.15.0
vtk>=9.3.0
# 3D Globe dependencies (Phase 1)
pydeck
simplekml
py3dtiles
matplotlib
numpy