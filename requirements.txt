# GeoAnomalyMapper relies on the dependencies declared in pyproject.toml.
# This file is provided for tooling that expects a requirements.txt entry point.
# Install the project (and optional extras) with:
#   pip install -e .[all]

-e .[all]
