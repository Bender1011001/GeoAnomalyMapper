# Environment configuration template for GAM Docker deployment
# Copy this to .env and customize as needed
# Usage: cp .env.example .env && docker compose up -d

# Deployment environment (production|development)
GAM_ENV=production

# API URL for dashboard (internal: http://gam-api:8000; external dev: http://localhost:8000)
GAM_API_URL=http://gam-api:8000

# Logging level (DEBUG|INFO|WARNING|ERROR)
LOG_LEVEL=INFO

# Uvicorn settings for API service
UVICORN_WORKERS=4
UVICORN_HOST=0.0.0.0
UVICORN_PORT=8000

# Streamlit settings for dashboard service
STREAMLIT_SERVER_PORT=8501
STREAMLIT_SERVER_ADDRESS=0.0.0.0

# Optional: Custom paths (absolute host paths for volumes; defaults to named volumes)
# DATA_PATH=/host/path/to/gam-data
# RESULTS_PATH=/host/path/to/gam-results

# Optional: Database (if GAM pipeline uses DB in future; currently file-based)
# POSTGRES_PASSWORD=secure_password
# DATABASE_URL=postgresql://user:pass@host:5432/db

# Production security (optional)
# SECRET_KEY=your-secret-key-for-any-future-auth